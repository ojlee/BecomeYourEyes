<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leeohju.becomeyoureyes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowBackground="@color/colorBackground">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/main_name"
            />
        <activity
            android:name=".GuideActivity"
            android:label="@string/guide_name"
            />
        <activity
            android:name=".CosActivity"
            android:label="@string/cos_name"
            />
        <activity
            android:name=".EmeActivity"
            android:label="@string/eme_name"
            />
        <activity
            android:name=".SetActivity"
            android:label="@string/set_name"
            />
        <activity
            android:name=".LocActivity"
            android:label="@string/reg_name">

        </activity>
    </application>

</manifest>